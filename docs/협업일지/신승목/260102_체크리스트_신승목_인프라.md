# 신승목님 체크리스트
## 인프라 (Docker, 배포, 테스트, 유틸리티)

**담당 범위**: 
- 유틸리티 모듈 (config, validators, storage, logging)
- 테스트 코드 작성 및 실행
- Docker 컨테이너화
- 배포 스크립트 및 자동화

**프로젝트 기간**: 2025-12-29 ~ 2026-01-27

---

## 📅 1단계 (MVP): ~ 2026-01-15

### 🗓 1주차 (12/29 ~ 1/4): GCP VM 및 파일 저장

#### Day 1-3 (12/29-12/31): GCP VM 환경 구성

**VM 인스턴스 확인**
- [ ] SSH 접속
  ```bash
  ssh user@{external-ip}
  ```
- [ ] 사양 확인
  - [ ] g2-standard-4 (vCPU 4, 메모리 16GB)
  - [ ] NVIDIA L4 GPU
  - [ ] 200GB 디스크
- [ ] GPU 확인
  ```bash
  nvidia-smi
  # NVIDIA L4, Driver 570.x, CUDA 12.6 확인
  ```

**기본 패키지 설치**
- [ ] 시스템 업데이트
  ```bash
  sudo apt-get update
  sudo apt-get upgrade -y
  ```
- [ ] Python 3.10 설치
  ```bash
  sudo apt-get install -y python3.10 python3.10-venv python3-pip
  ```
- [ ] Git 설치
  ```bash
  sudo apt-get install -y git
  ```
- [ ] PostgreSQL 설치 (2번 담당자 협업)
  ```bash
  sudo apt-get install -y postgresql postgresql-contrib
  ```

**방화벽 규칙**
- [ ] 포트 8000 (FastAPI) 허용
  ```bash
  sudo ufw allow 8000/tcp
  ```
- [ ] 포트 5432 (PostgreSQL) 외부 차단 확인
  ```bash
  sudo ufw status
  ```

**프로젝트 디렉토리 생성**
- [ ] 디렉토리 구조 생성
  ```bash
  cd ~
  mkdir -p ad-generator/src/{backend,generation,utils,deployment,tests}
  mkdir -p ad-generator/src/frontend/src/lib/{components,api}
  mkdir -p ad-generator/src/deployment/{docker,scripts}
  mkdir -p ad-generator/src/generation/{image_generation,text_generation}
  mkdir -p ad-generator/{storage,logs,models}
  mkdir -p ad-generator/storage/{input_images,generated_images}
  mkdir -p ad-generator/models/sdxl
  ```
- [ ] __init__.py 파일 생성
  ```bash
  cd ~/ad-generator
  touch src/__init__.py
  touch src/backend/__init__.py
  touch src/generation/__init__.py
  touch src/generation/{image_generation,text_generation}/__init__.py
  touch src/utils/__init__.py
  touch src/deployment/__init__.py
  touch src/tests/__init__.py
  ```

#### Day 4-7 (1/1-1/4): 유틸리티 모듈 구현

**config.py (src/utils/)**
- [ ] 파일 생성: `src/utils/config.py`
- [ ] 환경 설정 클래스
  ```python
  from pydantic_settings import BaseSettings
  
  class Settings(BaseSettings):
      # 데이터베이스
      DATABASE_URL: str
      DB_PASSWORD: str
      
      # OpenAI API
      OPENAI_API_KEY: str
      
      # JWT
      SECRET_KEY: str
      
      # 파일 저장
      STORAGE_PATH: str = "/home/user/ad-generator/storage/"
      
      # 로깅
      LOG_LEVEL: str = "INFO"
      
      # FastAPI
      DEBUG: bool = False
      ALLOWED_ORIGINS: str = "http://localhost:5173"
      
      class Config:
          env_file = ".env"
  
  settings = Settings()
  ```
- [ ] .env 파일 생성
  ```bash
  cat > .env << EOL
  DATABASE_URL=postgresql://ad_user:password@localhost:5432/ad_generator
  DB_PASSWORD=password
  OPENAI_API_KEY=
  SECRET_KEY=$(openssl rand -hex 32)
  STORAGE_PATH=/home/user/ad-generator/storage/
  LOG_LEVEL=INFO
  DEBUG=False
  ALLOWED_ORIGINS=http://localhost:5173
  EOL
  ```

**validators.py (src/utils/)**
- [ ] 파일 생성: `src/utils/validators.py`
- [ ] validate_login_id() 함수
  ```python
  import re
  
  def validate_login_id(login_id):
      if len(login_id) < 3 or len(login_id) > 20:
          return False
      
      pattern = r'^[a-zA-Z0-9]+$'
      return bool(re.match(pattern, login_id))
  ```
- [ ] validate_password() 함수
  ```python
  def validate_password(password):
      if len(password) < 8 or len(password) > 30:
          return False
      
      has_letter = bool(re.search(r'[a-zA-Z]', password))
      has_digit = bool(re.search(r'\d', password))
      
      return has_letter and has_digit
  ```
- [ ] 테스트
  ```python
  print(validate_login_id("user123"))  # True
  print(validate_login_id("ab"))  # False
  print(validate_password("pass1234"))  # True
  print(validate_password("short"))  # False
  ```

**storage.py (src/utils/)**
- [ ] 파일 생성: `src/utils/storage.py`
- [ ] save_image() 함수
  ```python
  import os
  import shutil
  import hashlib
  from datetime import datetime
  from src.backend.db_processor import DBProcessor
  from src.utils.config import settings
  import logging
  
  logger = logging.getLogger(__name__)
  db = DBProcessor()
  
  def save_image(image_path, user_id):
      # 1. 파일 해시 계산
      file_hash = calculate_hash(image_path)
      
      # 2. 저장 경로 결정
      timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
      user_dir = str(user_id) if user_id else "guest"
      
      relative_path = f"generated_images/{user_dir}/{timestamp}_{file_hash}.png"
      absolute_path = os.path.join(settings.STORAGE_PATH, relative_path)
      
      # 3. 디렉토리 생성
      os.makedirs(os.path.dirname(absolute_path), exist_ok=True)
      
      # 4. 파일 복사
      shutil.copy(image_path, absolute_path)
      
      logger.info(f"Image saved: {relative_path}")
      
      # 5. DB 저장 (image_matching)
      db.save_image_matching(file_hash, relative_path, "generated")
      
      return {
          "file_hash": file_hash,
          "file_path": absolute_path,
          "url": relative_path
      }
  ```
- [ ] calculate_hash() 함수
  ```python
  def calculate_hash(file_path):
      sha256 = hashlib.sha256()
      
      with open(file_path, 'rb') as f:
          while chunk := f.read(8192):
              sha256.update(chunk)
      
      return sha256.hexdigest()
  ```

**logging_config.py (src/utils/)**
- [ ] 파일 생성: `src/utils/logging_config.py`
- [ ] setup_logging() 함수
  ```python
  import logging
  import os
  from src.utils.config import settings
  
  def setup_logging():
      log_dir = "logs"
      os.makedirs(log_dir, exist_ok=True)
      
      logging.basicConfig(
          level=getattr(logging, settings.LOG_LEVEL),
          format='%(asctime)s [%(levelname)s] %(name)s: %(message)s',
          handlers=[
              logging.FileHandler(f"{log_dir}/app.log"),
              logging.StreamHandler()
          ]
      )
      
      logger = logging.getLogger(__name__)
      logger.info("Logging configured")
  ```
- [ ] 실행 테스트
  ```python
  from src.utils.logging_config import setup_logging
  setup_logging()
  
  import logging
  logger = logging.getLogger(__name__)
  logger.info("Test log")
  ```

**database.py (src/utils/)** (선택)
- [ ] DB 연결 풀 관리
- [ ] 트랜잭션 헬퍼 함수

**✅ 1주차 마일스톤**: 유틸리티 모듈 및 파일 저장 구현 완료

---

### 🗓 2주차 (1/5 ~ 1/11): FastAPI 서버 실행 및 테스트 준비

#### Day 8-10 (1/5-1/7): FastAPI 서버 실행

**uvicorn 실행**
- [ ] 서버 실행 테스트
  ```bash
  cd ~/ad-generator
  uvicorn src.backend.routes:app --host 0.0.0.0 --port 8000 --reload
  ```
- [ ] 외부 접속 테스트
  - [ ] 로컬에서: http://localhost:8000/docs
  - [ ] 외부에서: http://{VM_IP}:8000/docs

**PYTHONPATH 설정** (필요 시)
- [ ] 환경 변수 추가
  ```bash
  echo 'export PYTHONPATH=/home/user/ad-generator:$PYTHONPATH' >> ~/.bashrc
  source ~/.bashrc
  ```

**CORS 설정 확인**
- [ ] routes.py에서 CORS 미들웨어 확인
- [ ] 프론트엔드에서 API 호출 테스트

#### Day 11-14 (1/8-1/11): 기본 테스트 환경 구성

**pytest 설치**
- [ ] 의존성 추가
  ```bash
  pip install pytest pytest-asyncio httpx
  ```

**test_api.py (src/tests/)**
- [ ] 파일 생성: `src/tests/test_api.py`
- [ ] 기본 테스트 작성
  ```python
  import pytest
  from fastapi.testclient import TestClient
  from src.backend.routes import app
  
  client = TestClient(app)
  
  def test_root():
      response = client.get("/")
      assert response.status_code == 200
  
  def test_generate_endpoint():
      response = client.post("/generate", json={
          "user_id": None,
          "input_text": "카페 신메뉴 홍보",
          "aspect_ratio": "1:1"
      })
      assert response.status_code == 200
      assert "task_id" in response.json()
  
  def test_signup():
      response = client.post("/auth/signup", json={
          "login_id": "testuser123",
          "login_pw": "password123"
      })
      assert response.status_code == 200
      data = response.json()
      assert "success" in data
  ```

**테스트 실행**
- [ ] pytest 실행
  ```bash
  cd ~/ad-generator
  pytest src/tests/test_api.py -v
  ```

**✅ 2주차 마일스톤**: FastAPI 서버 실행 및 기본 테스트 완료

---

### 🗓 3주차 (1/12 ~ 1/15): MVP 배포

#### Day 15-17 (1/12-1/14): 배포 준비

**환경 변수 프로덕션 설정**
- [ ] .env 파일 수정
  ```bash
  DEBUG=False
  ALLOWED_ORIGINS=https://your-frontend.vercel.app
  ```

**FastAPI 서버 안정화**
- [ ] tmux 세션 생성
  ```bash
  tmux new -s fastapi
  cd ~/ad-generator
  uvicorn src.backend.routes:app --host 0.0.0.0 --port 8000
  # Ctrl+B, D로 세션 분리
  ```
- [ ] 세션 재접속
  ```bash
  tmux attach -t fastapi
  ```

**로그 모니터링**
- [ ] 로그 확인
  ```bash
  tail -f ~/ad-generator/logs/app.log
  ```

#### Day 18 (1/15): 프론트엔드 배포 지원 (1번 담당자 협업)

**Vercel 환경 변수 설정**
- [ ] VITE_API_URL 확인
  - [ ] http://{GCP_VM_IP}:8000

**최종 통합 테스트**
- [ ] 프론트엔드 → 백엔드 API 호출
- [ ] 광고 생성 전체 플로우
- [ ] 이미지 파일 서빙 확인

**✅ 3주차 마일스톤 (MVP 완료)**: 배포 완료

---

## 📅 2단계 (개선): 2026-01-16 ~ 27

### 🗓 4주차 (1/16 ~ 1/22): Docker 및 고급 테스트

#### Day 19-21 (1/16-1/18): Docker 컨테이너화

**Dockerfile.backend 작성**
- [ ] 파일 생성: `src/deployment/docker/Dockerfile.backend`
  ```dockerfile
  FROM python:3.10-slim
  
  # CUDA 지원 (GPU 사용 시)
  # FROM nvidia/cuda:12.1.0-runtime-ubuntu22.04
  
  WORKDIR /app
  
  # 의존성 설치
  COPY requirements.txt .
  RUN pip install --no-cache-dir -r requirements.txt
  
  # 소스 코드 복사
  COPY src/ ./src/
  COPY .env .
  
  # 포트 노출
  EXPOSE 8000
  
  # 실행
  CMD ["uvicorn", "src.backend.routes:app", "--host", "0.0.0.0", "--port", "8000"]
  ```

**Dockerfile.frontend 작성**
- [ ] 파일 생성: `src/deployment/docker/Dockerfile.frontend`
  ```dockerfile
  FROM node:18-alpine
  
  WORKDIR /app
  
  # 의존성 설치
  COPY src/frontend/package*.json ./
  RUN npm install
  
  # 소스 코드 복사
  COPY src/frontend/ .
  
  # 빌드
  RUN npm run build
  
  # 서빙
  RUN npm install -g serve
  CMD ["serve", "-s", "dist", "-l", "3000"]
  ```

**docker-compose.yml 작성**
- [ ] 파일 생성: `src/deployment/docker/docker-compose.yml`
  ```yaml
  version: '3.8'
  
  services:
    backend:
      build:
        context: ../../..
        dockerfile: src/deployment/docker/Dockerfile.backend
      ports:
        - "8000:8000"
      environment:
        - DATABASE_URL=postgresql://ad_user:password@postgres:5432/ad_generator
      depends_on:
        - postgres
      volumes:
        - ../../storage:/app/storage
        - ../../logs:/app/logs
    
    postgres:
      image: postgres:15
      environment:
        POSTGRES_DB: ad_generator
        POSTGRES_USER: ad_user
        POSTGRES_PASSWORD: password
      ports:
        - "5432:5432"
      volumes:
        - postgres_data:/var/lib/postgresql/data
    
    frontend:
      build:
        context: ../../..
        dockerfile: src/deployment/docker/Dockerfile.frontend
      ports:
        - "3000:3000"
      environment:
        - VITE_API_URL=http://localhost:8000
  
  volumes:
    postgres_data:
  ```

**Docker 빌드 및 실행**
- [ ] 이미지 빌드
  ```bash
  cd ~/ad-generator/src/deployment/docker
  docker-compose build
  ```
- [ ] 컨테이너 실행
  ```bash
  docker-compose up -d
  ```
- [ ] 로그 확인
  ```bash
  docker-compose logs -f backend
  ```

#### Day 22-24 (1/19-1/21): 고급 테스트

**test_models.py (src/tests/)**
- [ ] 파일 생성: `src/tests/test_models.py`
- [ ] SDXL 모델 테스트
  ```python
  import pytest
  from src.generation.image_generation.unified_generator import UnifiedImageGenerator
  
  @pytest.fixture
  def generator():
      return UnifiedImageGenerator(model_type="sdxl")
  
  def test_image_generation(generator):
      result = generator.generate(
          prompt="cafe interior, warm lighting",
          aspect_ratio="1:1"
      )
      
      assert "image_path" in result
      assert "metadata" in result
      assert os.path.exists(result["image_path"])
  ```
- [ ] GPT API 테스트
  ```python
  from src.generation.text_generation.text_generator import TextGenerator
  
  def test_ad_copy_generation():
      gen = TextGenerator(api_key=settings.OPENAI_API_KEY)
      copy = gen.generate_ad_copy("카페 신메뉴 홍보")
      
      assert isinstance(copy, str)
      assert len(copy) > 0
      assert len(copy) <= 30
  ```

**test_integration.py (src/tests/)**
- [ ] 파일 생성: `src/tests/test_integration.py`
- [ ] 전체 플로우 테스트
  ```python
  def test_full_workflow():
      # 1. 회원가입
      signup_response = client.post("/auth/signup", json={
          "login_id": "integtest",
          "login_pw": "password123"
      })
      assert signup_response.json()["success"]
      
      # 2. 로그인
      login_response = client.post("/auth/login", json={
          "login_id": "integtest",
          "login_pw": "password123"
      })
      user_id = login_response.json()["user_id"]
      
      # 3. 광고 생성
      gen_response = client.post("/generate", json={
          "user_id": user_id,
          "input_text": "카페 신메뉴",
          "aspect_ratio": "1:1"
      })
      task_id = gen_response.json()["task_id"]
      
      # 4. 상태 확인 (폴링)
      import time
      for _ in range(60):
          status_response = client.get(f"/status/{task_id}")
          status = status_response.json()["status"]
          
          if status == "completed":
              break
          
          time.sleep(1)
      
      assert status == "completed"
      
      # 5. 이력 조회
      history_response = client.get(f"/history/generation?user_id={user_id}")
      assert len(history_response.json()) > 0
  ```

**테스트 커버리지**
- [ ] pytest-cov 설치
  ```bash
  pip install pytest-cov
  ```
- [ ] 커버리지 측정
  ```bash
  pytest --cov=src --cov-report=html
  ```

### 🗓 5주차 (1/23 ~ 1/27): 배포 자동화 및 최종 마무리

#### Day 25-27 (1/23-1/25): 배포 스크립트

**deploy.sh 작성**
- [ ] 파일 생성: `src/deployment/scripts/deploy.sh`
  ```bash
  #!/bin/bash
  
  echo "=== Ad Generator Deployment Script ==="
  
  # 1. Git pull
  cd ~/ad-generator
  git pull origin main
  
  # 2. 의존성 설치
  source venv/bin/activate
  pip install -r requirements.txt
  
  # 3. DB 마이그레이션 (필요 시)
  # python scripts/migrate.py
  
  # 4. FastAPI 서버 재시작
  tmux kill-session -t fastapi 2>/dev/null
  tmux new -d -s fastapi "cd ~/ad-generator && uvicorn src.backend.routes:app --host 0.0.0.0 --port 8000"
  
  echo "=== Deployment Complete ==="
  ```
- [ ] 실행 권한 부여
  ```bash
  chmod +x src/deployment/scripts/deploy.sh
  ```

**systemd 서비스 등록** (선택)
- [ ] 서비스 파일 작성
  ```bash
  sudo nano /etc/systemd/system/ad-generator.service
  ```
  ```ini
  [Unit]
  Description=Ad Generator FastAPI Service
  After=network.target
  
  [Service]
  Type=simple
  User=user
  WorkingDirectory=/home/user/ad-generator
  Environment="PATH=/home/user/ad-generator/venv/bin"
  ExecStart=/home/user/ad-generator/venv/bin/uvicorn src.backend.routes:app --host 0.0.0.0 --port 8000
  Restart=always
  
  [Install]
  WantedBy=multi-user.target
  ```
- [ ] 서비스 시작
  ```bash
  sudo systemctl daemon-reload
  sudo systemctl enable ad-generator
  sudo systemctl start ad-generator
  sudo systemctl status ad-generator
  ```

#### Day 28-30 (1/26-1/27): 모니터링 및 문서화

**로그 로테이션**
- [ ] logrotate 설정
  ```bash
  sudo nano /etc/logrotate.d/ad-generator
  ```
  ```
  /home/user/ad-generator/logs/*.log {
      daily
      rotate 7
      compress
      missingok
      notifempty
  }
  ```

**모니터링 스크립트**
- [ ] health_check.sh
  ```bash
  #!/bin/bash
  
  # FastAPI 헬스 체크
  response=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/docs)
  
  if [ $response -ne 200 ]; then
      echo "FastAPI is down! Restarting..."
      systemctl restart ad-generator
  fi
  ```
- [ ] cron 등록
  ```bash
  crontab -e
  # */5 * * * * /home/user/ad-generator/src/deployment/scripts/health_check.sh
  ```

**문서화**
- [ ] 인프라 가이드 작성
  - [ ] VM 설정 방법
  - [ ] 배포 방법
  - [ ] 모니터링 방법
- [ ] 테스트 가이드 작성
  - [ ] 테스트 실행 방법
  - [ ] 커버리지 확인 방법

**✅ 최종 완료**: 인프라 구성 및 배포 자동화 완료

---

## 📊 진행 상황 추적

### 1단계 (MVP) 요약
- [ ] GCP VM 환경 구성
- [ ] 유틸리티 모듈 (config, validators, storage, logging)
- [ ] FastAPI 서버 실행
- [ ] 기본 테스트 (test_api.py)

### 2단계 (개선) 요약
- [ ] Docker 컨테이너화
- [ ] 고급 테스트 (test_models.py, test_integration.py)
- [ ] 배포 스크립트 (deploy.sh)
- [ ] 모니터링 및 문서화

---

## 🤝 협업 포인트

### 전체 팀원과의 협업
- [ ] 디렉토리 구조 공유
- [ ] 환경 변수 설정 공유
- [ ] 테스트 코드 작성 지원

### 진수경님과의 협업
- [ ] PostgreSQL 설정
- [ ] DB 백업 스크립트

### 이유노님과의 협업
- [ ] Vercel 배포 지원
- [ ] CORS 설정

---

## 📝 참고 사항

### 유용한 명령어
```bash
# tmux 세션 목록
tmux ls

# 세션 재접속
tmux attach -t fastapi

# 세션 종료
tmux kill-session -t fastapi

# 로그 실시간 확인
tail -f logs/app.log

# 디스크 사용량
df -h

# GPU 확인
nvidia-smi
```

### 테스트 명령어
```bash
# 전체 테스트
pytest

# 특정 파일 테스트
pytest src/tests/test_api.py -v

# 커버리지 측정
pytest --cov=src --cov-report=html
```

---

**작성일**: 2026-01-02
**담당자**: 신승목님 (인프라)  
**최종 수정**: 2026-01-02